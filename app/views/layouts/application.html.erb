<!DOCTYPE html>
<html>
<head>
  <title>Rainforest</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav>
    <div class="nav">
        <div class="container">
          <div class b= "row">
         <div class="col-md-4">
           </div>
        </div>
             <div class = "col-md-4">
               </div>
               <div class = "col-md-4">
               </div>
              <div class = "navtitles">
            <ul class = "pull-left">
                  <li><a href = "/products/new">Create a Product</a></li>
                  <li><a href="/products">View all Products</a></li>
                <!--   <li><a href="/contacts/show_contact">View a contact</a></li>
                  <li><a href="">Delete a contact</a></li>
                  <li><a href="">Display an attribute</a></li> -->
                  <li><a>
      <% if current_user %>
        Signed in as <%= current_user.name %>. <%= link_to "Log out", session_path("current"), :method => :delete %>
      <% else %>
        <%= link_to "Log in", new_session_path %> <%= link_to "Sign up", new_user_path %>
      <% end %>
    </a></li>
                </ul>
            </div>  
        </div>
    </div>
  </nav>

 
<%= yield %>

   <% if flash[:notice] %>
      <p id="notice"><%= flash[:notice] %></p>
    <% end %>
    <% if flash[:alert] %>
      <p id="alert"><%= flash[:alert] %></p>
    <% end %>


 <p class="userinfo">
      <% if current_user %>
        Signed in as <%= current_user.name %> <%= link_to "Log out", session_path("current"), :method => :delete %>
      <% else %>
        <%= link_to "Log in", new_session_path %> or <%= link_to "Sign up", new_user_path %>
      <% end %>
    </p>

</body>
</html>
